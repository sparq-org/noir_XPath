//! Unit tests for xpath library

fn main() {
    // This is a test binary - tests are run via `nargo test`
    println("xpath_unit_tests - run with `nargo test`");
}

mod numeric_tests;
mod datetime_tests;
mod date_tests;
mod boolean_tests;
mod string_tests;
mod hash_tests;
