//! Test chunk 3 for prod:AxisStep
//! Contains 50 tests

use dep::xpath::stub_prodaxisstep;

#[test]
fn axes076_2() {
    //  Path '//child::node()' from an element.
    // XPath: fn:count(//center//child::node())
    // Expected: 1
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes076_3() {
    //  Path '//child::node()' from an element.
    // XPath: fn:count(//center//child::node())
    // Expected: 1
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes076_4() {
    //  Path '//child::node()' from an element.
    // XPath: fn:count(//center//child::node())
    // Expected: 37
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes077_1() {
    //  Test implied child axis in '//*' after an element.
    // XPath: fn:count(//center//*)
    // Expected: 0
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes077_2() {
    //  Test implied child axis in '//*' after an element.
    // XPath: fn:count(//center//*)
    // Expected: 1
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes077_3() {
    //  Test implied child axis in '//*' after an element.
    // XPath: fn:count(//center//*)
    // Expected: 12
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes078_1() {
    //  Test implied child axis in element//element.
    // XPath: fn:count(//center//south)
    // Expected: 0
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes078_2() {
    //  Test implied child axis in element//element.
    // XPath: fn:count(//center//south)
    // Expected: 0
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes078_3() {
    //  Test implied child axis in element//element.
    // XPath: fn:count(//center//south)
    // Expected: 1
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes078_4() {
    //  Test implied child axis in element//element.
    // XPath: fn:count(//center//south)
    // Expected: 8
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes079_1() {
    //  Test implied child axis in '//node()' from an element.
    // XPath: fn:count(//center//node())
    // Expected: 0
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes079_2() {
    //  Test implied child axis in '//node()' from an element.
    // XPath: fn:count(//center//node())
    // Expected: 1
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes079_3() {
    //  Test implied child axis in '//node()' from an element.
    // XPath: fn:count(//center//node())
    // Expected: 1
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes079_4() {
    //  Test implied child axis in '//node()' from an element.
    // XPath: fn:count(//center//node())
    // Expected: 37
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes080_1() {
    //  Path '//attribute::*' after an element.
    // XPath: fn:count(//west//attribute::*)
    // Expected: 0
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes080_2() {
    //  Path '//attribute::*' after an element.
    // XPath: fn:count(//west//attribute::*)
    // Expected: 1
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes080_3() {
    //  Path '//attribute::*' after an element.
    // XPath: fn:count(//west//attribute::*)
    // Expected: 4
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes081_1() {
    //  Path '//attribute::' with name of attribute, after an element.
    // XPath: fn:count(//center//attribute::center-attr-2)
    // Expected: 0
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes081_2() {
    //  Path '//attribute::' with name of attribute, after an element.
    // XPath: fn:count(//center//attribute::center-attr-2)
    // Expected: 0
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes081_3() {
    //  Path '//attribute::' with name of attribute, after an element.
    // XPath: fn:count(//center//attribute::center-attr-2)
    // Expected: 1
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes081_4() {
    //  Path '//attribute::' with name of attribute, after an element.
    // XPath: fn:count(//center//attribute::center-attr-2)
    // Expected: 4
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes082_1() {
    //  Path '//attribute::node()' after an element.
    // XPath: fn:count(//west//attribute::node())
    // Expected: 0
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes082_2() {
    //  Path '//attribute::node()' after an element.
    // XPath: fn:count(//west//attribute::node())
    // Expected: 1
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes082_3() {
    //  Path '//attribute::node()' after an element.
    // XPath: fn:count(//west//attribute::node())
    // Expected: 4
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes083_1() {
    //  Path '//@*' (abbreviated syntax) after an element.
    // XPath: fn:count(//west//@*)
    // Expected: 0
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes083_2() {
    //  Path '//@*' (abbreviated syntax) after an element.
    // XPath: fn:count(//west//@*)
    // Expected: 1
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes083_3() {
    //  Path '//@*' (abbreviated syntax) after an element.
    // XPath: fn:count(//west//@*)
    // Expected: 4
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes084_1() {
    //  Path '//@' with name of attribute, after an element.
    // XPath: fn:count(//center//@center-attr-2)
    // Expected: 0
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes084_2() {
    //  Path '//@' with name of attribute, after an element.
    // XPath: fn:count(//center//@center-attr-2)
    // Expected: 0
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes084_3() {
    //  Path '//@' with name of attribute, after an element.
    // XPath: fn:count(//center//@center-attr-2)
    // Expected: 1
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes084_4() {
    //  Path '//@' with name of attribute, after an element.
    // XPath: fn:count(//center//@center-attr-2)
    // Expected: 4
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes084_5() {
    //  Use of //text() to get all text nodes.
    // XPath: fn:count(//text()[normalize-space()])
    // Expected: 827
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes102() {
    //  Assert that the static type of a wild name test on an element with a type annot
    // XPath: declare namespace ns1 = \"http://www.example.org/ns1\";
    // Expected: 1
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes111() {
    //  Assert that the static type of a wild name test on an attribute with a type ann
    // XPath: declare namespace ns1 = \"http://www.example.org/ns1\";
    // Expected: 1
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes121() {
    //  String value of a namespace node
    // XPath: string(/*/namespace::xlink)
    // Expected: 'http://www.w3.org/1999/xlink'
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes122() {
    //  Identity of a namespace node
    // XPath: /*/namespace::xlink is /*/*[1]/namespace::xlink
    // Expected: false
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn axes123() {
    //  Identity of a namespace node
    // XPath: /*/namespace::xlink is /*/namespace::*[. = 'http://www.w3.or
    // Expected: true
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn k2_axes_3() {
    //  Path expression where the last step in an xs:integer.
    // XPath: declare variable $v := <a><b/><b/><b/></a>; deep-equal($v//4
    // Expected: true
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn k2_axes_4() {
    //  Path expression where the last step in an xs:integer(#2).
    // XPath: declare variable $v := <a><b/><b/><b/></a>; $v//45
    // Expected: 45 45 45 45
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn k2_axes_18() {
    //  Apply the parent axis to a computed text constructor.
    // XPath: empty(text {\"some text\"}/..)
    // Expected: true
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn k2_axes_19() {
    //  Apply the parent axis to a computed processing instruction constructor.
    // XPath: empty(processing-instruction theName {\"some text\"}/..)
    // Expected: true
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn k2_axes_20() {
    //  Apply the parent axis to a computed attribute constructor.
    // XPath: empty(attribute theName {\"some text\"}/..)
    // Expected: true
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn k2_axes_21() {
    //  Apply the parent axis to a computed attribute constructor.
    // XPath: empty(element theName {\"some text\"}/..)
    // Expected: true
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn k2_axes_22() {
    //  Apply the parent axis to a computed comment constructor.
    // XPath: empty(comment {\"some text\"}/..)
    // Expected: true
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn k2_axes_23() {
    //  Apply the parent axis to a direct element constructor.
    // XPath: empty(<elem/>/..)
    // Expected: true
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn k2_axes_42() {
    //  Ensure a parent axis inside a predicate where the source is a direct element co
    // XPath: empty(<e/>[parent::node()])
    // Expected: true
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn k2_axes_47() {
    //  Apply '/' to a variable.
    // XPath: declare variable $myVar := <e/>; empty($myVar/descendant-or-
    // Expected: true
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn k2_axes_48() {
    //  Apply '/' to a variable, with an xs:integer at the end.
    // XPath: declare variable $myVar := <e/>; $myVar/(<a/>, <b/>, <?d ?>,
    // Expected: 3 3 3 3 3 3
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn k2_axes_49() {
    //  Apply '/' to a variable, with fn:number() at the end. That is, an implicit depe
    // XPath: declare variable $myVar := <e/>; $myVar/(<a/>, <b/>, <?d ?>,
    // Expected: NaN NaN NaN NaN NaN NaN
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}

#[test]
fn k2_axes_63() {
    //  Apply the child axis to an element who has one attribute, combined with the seq
    // XPath: 1, <b attr=\"fo\"/>/child::node(), 1
    // Expected: 1 1
    // Calls stub function - will fail until prod:AxisStep is implemented
    let _ = stub_prodaxisstep();
}
