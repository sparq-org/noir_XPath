//! Test chunk 11 for fn:matches.re
//! Contains 50 tests

use dep::xpath::stub_fnmatches_re;

#[test]
fn re00645() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:(f
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00655() {
    // Test regex syntax
    // XPath: (every $s in tokenize('foo? bar?', ',') satisfies matches($s
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00656() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:(f
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00657() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:([
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00658() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:((
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00659() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:((
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00660() {
    // Test regex syntax
    // XPath: (every $s in tokenize('aaabbbcccdddeeefff', ',') satisfies m
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00661() {
    // Test regex syntax
    // XPath: (every $s in tokenize('dddeeeccceee', ',') satisfies matches
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00662() {
    // Test regex syntax
    // XPath: (every $s in tokenize('dddeeeccceee', ',') satisfies matches
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00663() {
    // Test regex syntax
    // XPath: (every $s in tokenize('aaabbbcccdddeeefff', ',') satisfies m
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00664() {
    // Test regex syntax
    // XPath: (every $s in tokenize('dddeeeccceee', ',') satisfies matches
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00665() {
    // Test regex syntax
    // XPath: (every $s in tokenize('dddeeeccceee', ',') satisfies matches
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00666() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:CH
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00667() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:cH
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00668() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:AA
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00669() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:aA
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00670() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:?)
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00671() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:?)
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00672() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:([
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00673() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:([
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00689() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:ht
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00690() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:ht
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00691() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:([
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00692() {
    // Test regex syntax
    // XPath: (every $s in tokenize('foo', ',') satisfies matches($s, '^(?
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00693() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[a
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00694() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00695() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00696() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00697() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00698() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00699() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00700() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00701() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00702() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[a
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00703() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00704() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00705() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00706() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00707() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00708() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00709() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00710() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00711() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00712() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00713() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ';') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00714() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00715() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00716() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00717() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[a
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}

#[test]
fn re00718() {
    // Test regex syntax
    // XPath: (every $s in tokenize('', ',') satisfies matches($s, '^(?:[\
    // Expected: true
    // Calls stub function - will fail until fn:matches.re is implemented
    let _ = stub_fnmatches_re();
}
