//! Test chunk 0 for fn:unordered
//! Contains 1 tests

#[test]
fn fnunordered_no_converted_tests() {
    // Placeholder: 14 qt3tests cases could not be converted.
    assert(false, "No qt3tests cases could be converted for fn:unordered");
}
