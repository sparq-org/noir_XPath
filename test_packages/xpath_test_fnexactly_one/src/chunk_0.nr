//! Test chunk 0 for fn:exactly-one
//! Contains 1 tests

#[test]
fn fnexactly_one_no_converted_tests() {
    // Placeholder: 45 qt3tests cases could not be converted.
    assert(false, "No qt3tests cases could be converted for fn:exactly-one");
}
