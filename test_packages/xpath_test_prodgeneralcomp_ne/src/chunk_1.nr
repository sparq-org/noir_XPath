//! Test chunk 1 for prod:GeneralComp.ne
//! Contains 50 tests

use dep::xpath::stub_prodgeneralcomp_ne;

#[test]
fn generalexpression150() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) != 10000
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression151() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) != (50000)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression152() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) != (10000,50000)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression153() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) != <a>10000</a>
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression154() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) != (<a>10000</a>)
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression155() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) != (<a>10000</a>,<b>50000</b>)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression156() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) != (/works/employee[1]/hours[1])
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression157() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) != (/works/employee[1]/hours[1],/works/employ
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression158() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) != ($works/works/employee[1]/hours[1],$staff/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression159() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) != ()
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression160() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) != 10000
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression161() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) != (50000)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression162() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) != (10000,50000)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression163() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) != <a>10000</a>
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression164() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) != (<a>10000</a>)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression165() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) != (<a>10000</a>,<b>50000</b>)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression166() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) != (/works/employee[1]/hours[1])
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression167() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) != (/works/employee[1]/hours[1],
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression168() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) != ($works/works/employee[1]/hou
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression169() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) != ()
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression170() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) != 10000
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression171() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) != (50000)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression172() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) != (10000,50000)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression173() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) != <a>10000</a>
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression174() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) != (<a>10000</a>)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression175() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) != (<a>10000</a>,<b>50000</b>)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression176() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) != (/works/employee[1]/hours[1
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression177() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) != (/works/employee[1]/hours[1
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression178() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1]) != ($works/works/employe
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression179() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) !=
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression180() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) !=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression181() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) !=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression182() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) !=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression183() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) !=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression184() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) !=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression185() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) !=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression186() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) !=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression187() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) !=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression188() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$works/works/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression189() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression190() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression191() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression192() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression193() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression194() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression195() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression196() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression197() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn generalexpression198() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}

#[test]
fn k_gencompne_1() {
    //  General comparison where one or more operands is the empty sequence.
    // XPath: (() != ()) eq false()
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ne is implemented
    let _ = stub_prodgeneralcomp_ne();
}
