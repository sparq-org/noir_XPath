//! Test chunk 0 for op:string-less-than
//! Contains 1 tests

#[test]
fn opstring_less_than_no_converted_tests() {
    // Placeholder: 10 qt3tests cases could not be converted.
    assert(false, "No qt3tests cases could be converted for op:string-less-than");
}
