//! Test chunk 0 for fn:tail
//! Contains 1 tests

#[test]
fn fntail_no_converted_tests() {
    // Placeholder: 2 qt3tests cases could not be converted.
    assert(false, "No qt3tests cases could be converted for fn:tail");
}
