//! Test chunk 1 for prod:GeneralComp.ge
//! Contains 50 tests

use dep::xpath::stub_prodgeneralcomp_ge;

#[test]
fn generalexpression546() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) >= 10000
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression547() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) >= (50000)
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression548() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) >= (10000,50000)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression549() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) >= <a>10000</a>
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression550() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) >= (<a>10000</a>)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression551() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) >= (<a>10000</a>,<b>50000</b>)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression552() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) >= (/works/employee[1]/hours[1])
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression553() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) >= (/works/employee[1]/hours[1],/works/employ
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression554() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) >= ($works/works/employee[1]/hours[1],$staff/
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression555() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) >= ()
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression556() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) >= 10000
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression557() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) >= (50000)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression558() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) >= (10000,50000)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression559() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) >= <a>10000</a>
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression560() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) >= (<a>10000</a>)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression561() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) >= (<a>10000</a>,<b>50000</b>)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression562() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) >= (/works/employee[1]/hours[1])
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression563() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) >= (/works/employee[1]/hours[1],
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression564() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) >= ($works/works/employee[1]/hou
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression565() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) >= ()
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression566() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) >= 10000
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression567() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) >= (50000)
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression568() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) >= (10000,50000)
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression569() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) >= <a>10000</a>
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression570() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) >= (<a>10000</a>)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression571() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) >= (<a>10000</a>,<b>50000</b>)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression572() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) >= (/works/employee[1]/hours[1
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression573() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) >= (/works/employee[1]/hours[1
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression574() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1]) >= ($works/works/employe
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression575() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) >=
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression576() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) >=
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression577() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) >=
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression578() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) >=
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression579() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) >=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression580() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) >=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression581() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) >=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression582() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) >=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression583() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) >=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression584() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$works/works/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression585() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression586() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression587() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression588() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression589() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression590() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression591() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression592() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression593() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression594() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}

#[test]
fn generalexpression596() {
    //  Test of general comparison with non-existing attribute
    // XPath: count(document{<T/>}//*[exists(@n1 >= -1)])
    // Expected: 1
    // Calls stub function - will fail until prod:GeneralComp.ge is implemented
    let _ = stub_prodgeneralcomp_ge();
}
