//! Test chunk 0 for fn:string-to-codepoints
//! Contains 1 tests

#[test]
fn fnstring_to_codepoints_no_converted_tests() {
    // Placeholder: 28 qt3tests cases could not be converted.
    assert(false, "No qt3tests cases could be converted for fn:string-to-codepoints");
}
