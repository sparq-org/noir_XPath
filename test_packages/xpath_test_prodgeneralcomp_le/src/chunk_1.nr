//! Test chunk 1 for prod:GeneralComp.le
//! Contains 50 tests

use dep::xpath::stub_prodgeneralcomp_le;

#[test]
fn generalexpression348() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) <= 10000
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression349() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) <= (50000)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression350() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) <= (10000,50000)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression351() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) <= <a>10000</a>
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression352() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) <= (<a>10000</a>)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression353() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) <= (<a>10000</a>,<b>50000</b>)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression354() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) <= (/works/employee[1]/hours[1])
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression355() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) <= (/works/employee[1]/hours[1],/works/employ
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression356() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>) <= ($works/works/employee[1]/hours[1],$staff/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression357() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) <= ()
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression358() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) <= 10000
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression359() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) <= (50000)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression360() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) <= (10000,50000)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression361() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) <= <a>10000</a>
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression362() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) <= (<a>10000</a>)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression363() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) <= (<a>10000</a>,<b>50000</b>)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression364() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) <= (/works/employee[1]/hours[1])
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression365() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) <= (/works/employee[1]/hours[1],
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression366() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (<a>10000</a>,<b>50000</b>) <= ($works/works/employee[1]/hou
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression367() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) <= ()
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression368() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) <= 10000
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression369() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) <= (50000)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression370() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) <= (10000,50000)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression371() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) <= <a>10000</a>
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression372() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) <= (<a>10000</a>)
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression373() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) <= (<a>10000</a>,<b>50000</b>)
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression374() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) <= (/works/employee[1]/hours[1
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression375() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1]) <= (/works/employee[1]/hours[1
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression376() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1]) <= ($works/works/employe
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression377() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) <=
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression378() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) <=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression379() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) <=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression380() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) <=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression381() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) <=
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression382() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) <=
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression383() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) <=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression384() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) <=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression385() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: (/works/employee[1]/hours[1],/works/employee[6]/hours[1]) <=
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression386() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$works/works/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression387() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression388() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression389() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression390() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression391() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression392() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: false
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression393() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression394() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression395() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn generalexpression396() {
    //  Test of a General Expression with the operands set as follows operand1 = Sequen
    // XPath: ($works/works/employee[1]/hours[1],$staff/staff/employee[6]/
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}

#[test]
fn lessthaneqonanyuri_1() {
    // Test - lessthaneqonanyuri-1  Evaluation of 'le' operator on xs:anyURI datatype.
    // XPath: (xs:anyURI(\"http://www.example/com\")) <= (xs:anyURI(\"http
    // Expected: true
    // Calls stub function - will fail until prod:GeneralComp.le is implemented
    let _ = stub_prodgeneralcomp_le();
}
