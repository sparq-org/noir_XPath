//! Test chunk 5 for prod:InstanceofExpr
//! Contains 26 tests

use dep::xpath::stub_prodinstanceofexpr;

#[test]
fn k2_seqexprinstanceof_76() {
    //  Ensure that a cast to xs:negativeInteger is of the right type, when casting fro
    // XPath: xs:negativeInteger(xs:double(\"-4\")) instance of xs:negativ
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_77() {
    //  Ensure that a cast to xs:long is of the right type, when casting from xs:double
    // XPath: xs:long(xs:double(\"-4\")) instance of xs:long
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_78() {
    //  Ensure that a cast to xs:int is of the right type, when casting from xs:double.
    // XPath: xs:int(xs:double(\"-4\")) instance of xs:int
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_79() {
    //  Ensure that a cast to xs:short is of the right type, when casting from xs:doubl
    // XPath: xs:short(xs:double(\"-4\")) instance of xs:short
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_80() {
    //  Ensure that a cast to xs:nonNegativeInteger is of the right type, when casting
    // XPath: xs:nonNegativeInteger(xs:double(\"4\")) instance of xs:nonNe
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_81() {
    //  Ensure that a cast to xs:unsignedLong is of the right type, when casting from x
    // XPath: xs:unsignedLong(xs:double(\"4\")) instance of xs:unsignedLon
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_82() {
    //  Ensure that a cast to xs:unsignedInt is of the right type, when casting from xs
    // XPath: xs:unsignedInt(xs:double(\"4\")) instance of xs:unsignedInt
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_83() {
    //  Ensure that a cast to xs:unsignedShort is of the right type, when casting from
    // XPath: xs:unsignedShort(xs:double(\"4\")) instance of xs:unsignedSh
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_84() {
    //  Ensure that a cast to xs:unsignedByte is of the right type, when casting from x
    // XPath: xs:unsignedByte(xs:double(\"4\")) instance of xs:unsignedByt
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_85() {
    //  Ensure that a cast to xs:positiveInteger is of the right type, when casting fro
    // XPath: xs:positiveInteger(xs:double(\"4\")) instance of xs:positive
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_86() {
    //  Ensure that a cast to xs:byte is of the right type, when casting from xs:float.
    // XPath: xs:byte(xs:float(\"1\")) instance of xs:byte
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_87() {
    //  Ensure that a cast to xs:nonPositiveInteger is of the right type, when casting
    // XPath: xs:nonPositiveInteger(xs:float(\"0\")) instance of xs:nonPos
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_88() {
    //  Ensure that a cast to xs:negativeInteger is of the right type, when casting fro
    // XPath: xs:negativeInteger(xs:float(\"-4\")) instance of xs:negative
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_89() {
    //  Ensure that a cast to xs:long is of the right type, when casting from xs:float.
    // XPath: xs:long(xs:float(\"-4\")) instance of xs:long
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_90() {
    //  Ensure that a cast to xs:int is of the right type, when casting from xs:float.
    // XPath: xs:int(xs:float(\"-4\")) instance of xs:int
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_91() {
    //  Ensure that a cast to xs:short is of the right type, when casting from xs:float
    // XPath: xs:short(xs:float(\"-4\")) instance of xs:short
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_92() {
    //  Ensure that a cast to xs:nonNegativeInteger is of the right type, when casting
    // XPath: xs:nonNegativeInteger(xs:float(\"4\")) instance of xs:nonNeg
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_93() {
    //  Ensure that a cast to xs:unsignedLong is of the right type, when casting from x
    // XPath: xs:unsignedLong(xs:float(\"4\")) instance of xs:unsignedLong
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_94() {
    //  Ensure that a cast to xs:unsignedInt is of the right type, when casting from xs
    // XPath: xs:unsignedInt(xs:float(\"4\")) instance of xs:unsignedInt
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_95() {
    //  Ensure that a cast to xs:unsignedShort is of the right type, when casting from
    // XPath: xs:unsignedShort(xs:float(\"4\")) instance of xs:unsignedSho
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_96() {
    //  Ensure that a cast to xs:unsignedByte is of the right type, when casting from x
    // XPath: xs:unsignedByte(xs:float(\"4\")) instance of xs:unsignedByte
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_97() {
    //  Ensure that a cast to xs:positiveInteger is of the right type, when casting fro
    // XPath: xs:positiveInteger(xs:float(\"4\")) instance of xs:positiveI
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_98() {
    //  Instance of involving empty-sequence().
    // XPath: node-name(text {\"\"}) instance of empty-sequence()
    // Expected: true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_99() {
    //  Test using attribute().
    // XPath: <e attr=\"\"/>/(@attr instance of attribute(), @* instance o
    // Expected: true true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn k2_seqexprinstanceof_100() {
    //  Test using element().
    // XPath: <a><b/></a>/(b instance of element(), * instance of element(
    // Expected: true true
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}

#[test]
fn cbcl_instance_of_001() {
    //  Test that the types of variables are correctly preserved on the stack.
    // XPath: for $x in (xs:integer(1), xs:decimal(1)) return (((($x + 1)
    // Expected: true false
    // Calls stub function - will fail until prod:InstanceofExpr is implemented
    let _ = stub_prodinstanceofexpr();
}
